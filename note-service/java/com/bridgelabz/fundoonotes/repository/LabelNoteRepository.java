package com.bridgelabz.fundoonotes.repository;

import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.data.jpa.repository.Query;
import org.springframework.stereotype.Repository;

import com.bridgelabz.fundoonotes.model.LabelNote;
//@Repository
//public interface LabelNoteRepository  extends JpaRepository<LabelNote, Long>{
//    @Query("select * from LabelNote where noteId=? AND labelId=?")
//	LabelNote findLabelbyNoteId(Long noteId,Long labelId);
//
//}    
